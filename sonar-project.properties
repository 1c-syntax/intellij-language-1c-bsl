sonar.projectKey=1c-syntax_intellij-language-1c-bsl
sonar.organization=1c-syntax

# TextMate bundle is a vendored VSCode-style grammar package (package.json + *.tmLanguage.json),
# not an npm project, so lock-file / dependency rules do not apply to it.
sonar.exclusions=**/vendor/**/*.*, **/gen/**/*.*, **/textmate/**/*.*
